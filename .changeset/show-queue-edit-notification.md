---
'kimaki': patch
---

Show a notification in the Discord thread when a user edits or removes a queued message.

- Editing a queued message shows: `⬦ **username** edited queued message`
- Removing queue suffix (un-queuing) shows: `⬦ **username** removed message from queue`
