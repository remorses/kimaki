---
'kimaki': patch
'website': patch
---

Explain that session titles prefixed `btw:` are side-question forks, not duplicate sessions.

`/last-sessions` and `kimaki session list --active` can show a `btw:` thread next to the original task. That thread only answers a related question in parallel. It is not a second copy of the same session.
