---
'kimaki': minor
---

Add a public guide for debugging sessions and filing Kimaki GitHub issues, and include the exact `providerID/modelID` in `kimaki session read` output.

Agents now get a system-prompt link to https://kimaki.dev/docs/guides/report-bugs. The page covers `kimaki session export-events-jsonl`, sharing jsonl, logs, session markdown, and reproduction code in a secret gist, and never opening a PR unless remorses agrees on the issue.
