---
'kimaki': minor
---

Add `--skip-footer-mentions` to run Kimaki without mentioning thread creators in final session footers.

Use this flag when completion messages should stay visible in the thread without creating mention notifications:

```sh
kimaki --skip-footer-mentions
```

Footer mentions remain enabled by default.
