---
'kimaki': patch
---

Keep completion footers silent when the current turn uses `kimaki_sleep`.

The waiting footer no longer mentions or notifies the user. When the scheduled wake later completes, its footer still sends the normal notification.
