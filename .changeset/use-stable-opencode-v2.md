---
'kimaki': patch
---

Use the published OpenCode 2.0.2 CLI, client, and plugin packages under the `@opencode` scope. Resolve the installed native executable so sessions use the matching server version, including native session permissions and the 256 KiB instruction-entry limit.

Related to #220.
