---
'kimaki': patch
---

Resume the original voice note automatically after the user saves the OpenAI or Gemini API key requested by Kimaki.

The first voice note now stays pending while the API key modal is open. After the key is saved, Kimaki transcribes and sends that same note without asking the user to record or upload it again.
