// Side chat. ". btw" forks a new Discord thread. session.fork is not on Plugin.Context.

import { Plugin } from '@opencode-ai/plugin'

export default Plugin.define({
  id: 'kimaki.btw',
  async setup() {},
})
